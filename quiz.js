// Write a JavaScript function that checks whether a passed string is palindrome or not

// A string is palindrome if it is read the same from forward or backword

function stringTop(){
   for ( i = 0; i < array.length; i++) {
    let i
    
   } 
}